import { Badge } from "@/components/ui/badge";
import { Brain, Target, Zap, Sparkles, Lightbulb, AlertTriangle, CheckCircle2, Info, Play, BookOpen, Code2, ExternalLink, Cpu, TrendingUp } from "lucide-react";

# 第四周：训练微型 GPT——让模型「学会」生成文字

<Badge variant="outline">模块四 · 第 4/4 周</Badge> <Badge variant="secondary" className="ml-2">预计 4 小时</Badge>

## 本周目标

整合前三周的知识，用莎士比亚文本训练一个字符级 GPT。观察 loss 下降、生成质量提升的过程。

**本周里程碑**：训练完成的微型 GPT 能生成「莎士比亚风格」的新文本——这是整个模块四的高光时刻。

---

## 1. GPT 训练全景

<ImageCard
  src="/images/concepts/gpt-training-process.png"
  alt="FLUX 生成：GPT 训练过程时间线"
  caption="从左到右是训练的进程：乱码 → 偶尔有真实的词 → 短语 → 语法正确的句子 → 莎士比亚风格文本。每个阶段都是一次进步——这就是 AI 「学会」语言的全过程。"
/>

<ImageCard
  src="/images/diagrams/gpt-architecture.svg"
  alt="微型 GPT 训练流程全景图"
  caption="从莎士比亚文本到能生成新文本的微型 GPT：准备数据 → 字符级Token化 → 训练循环 → 文本生成。每个步骤我们在前几周都已经分别学过了，这周把它们连起来。"
/>

### 训练流程

```
循环 N 个 Epoch:
  ├── 取一个批次的数据（64 个字符序列）
  ├── 前向传播 → 预测下一个字符
  ├── 计算 Cross-Entropy Loss
  ├── 反向传播 → 更新所有参数
  └── 每 100 步：生成一段样本文本，看看效果
```

<KeyPoint emoji="🎯">
  每次训练的目标：给定前 N 个字符，预测第 N+1 个字符。比如看到「To be, or not to 」，模型应该预测下一个字符是「b」（因为原文是「to be」）。
</KeyPoint>

---

## 2. 动手实践：训练你的微型 GPT

### 完整训练代码（使用 Google Colab 免费 GPU）

```python
import torch
import torch.nn as nn
from torch.nn import functional as F

# ---- 超参数 ----
batch_size = 64
block_size = 256   # 上下文长度
max_iters = 5000
eval_interval = 500
learning_rate = 3e-4
device = 'cuda' if torch.cuda.is_available() else 'cpu'
n_embd = 384       # Embedding 维度
n_head = 6         # 注意力头数
n_layer = 6        # Transformer 层数
dropout = 0.2

# ---- 数据准备 ----
with open('shakespeare.txt', 'r', encoding='utf-8') as f:
    text = f.read()

chars = sorted(list(set(text)))
vocab_size = len(chars)

# 字符 ↔ 数字 映射
stoi = {ch: i for i, ch in enumerate(chars)}
itos = {i: ch for i, ch in enumerate(chars)}
encode = lambda s: [stoi[c] for c in s]
decode = lambda l: ''.join([itos[i] for i in l])

data = torch.tensor(encode(text), dtype=torch.long)

# 训练/验证分割
n = int(0.9 * len(data))
train_data = data[:n]
val_data = data[n:]

def get_batch(split):
    data = train_data if split == 'train' else val_data
    ix = torch.randint(len(data) - block_size, (batch_size,))
    x = torch.stack([data[i:i+block_size] for i in ix])
    y = torch.stack([data[i+1:i+block_size+1] for i in ix])
    return x.to(device), y.to(device)

# ---- GPT 模型 ----
class Head(nn.Module):
    """单头 Self-Attention"""
    def __init__(self, head_size):
        super().__init__()
        self.key = nn.Linear(n_embd, head_size, bias=False)
        self.query = nn.Linear(n_embd, head_size, bias=False)
        self.value = nn.Linear(n_embd, head_size, bias=False)
        self.register_buffer('tril', torch.tril(torch.ones(block_size, block_size)))

    def forward(self, x):
        B, T, C = x.shape
        k = self.key(x)
        q = self.query(x)
        wei = q @ k.transpose(-2, -1) * C**-0.5
        wei = wei.masked_fill(self.tril[:T, :T] == 0, float('-inf'))
        wei = F.softmax(wei, dim=-1)
        v = self.value(x)
        return wei @ v

# (完整的 GPT 模型包含 MultiHeadAttention + FeedForward + Transformer Blocks
#  完整代码约200行，建议在 Colab 上跑完整版本
#  参考: https://github.com/karpathy/nanoGPT)

# ---- 训练循环 ----
model = GPT()  # 完整GPT模型
optimizer = torch.optim.AdamW(model.parameters(), lr=learning_rate)

for iter in range(max_iters):
    xb, yb = get_batch('train')
    logits, loss = model(xb, yb)
    optimizer.zero_grad(set_to_none=True)
    loss.backward()
    optimizer.step()

    if iter % eval_interval == 0:
        print(f"step {iter:5d}: loss {loss.item():.4f}")
        # 生成样本文本
        context = torch.zeros((1, 1), dtype=torch.long, device=device)
        print(decode(model.generate(context, max_new_tokens=200)[0].tolist()))
        print("---")
```

<SuccessBox title="如果你跑完了 5000 步训练">
  恭喜！！你现在完成了一件很多人都没做过的事——亲手训练了一个能生成文本的 AI 模型。

  Loss 应该从 ~4.0 降到了 ~1.5 左右。生成效果从乱码 → 偶尔有单词 → 语法基本正确 → 莎士比亚风格的短句。

  你的模型只有约 10M 参数（GPT-3 有 175B），用的数据只是莎士比亚全集（约 5MB）。但核心算法一模一样——scale up 就是 GPT-4。
</SuccessBox>

---

## 3. 模块四总结

<DataTable
  headers={["概念", "一句话解释", "你实现的"]}
  rows={[
    ["前向传播", "数据流过网络：矩阵乘法+激活", "NumPy 前向传播"],
    ["反向传播", "算梯度→更新参数：链式法则", "NumPy 完整训练"],
    ["梯度下降", "跟着坡度往下走：W -= lr × grad", "XOR 学习成功"],
    ["Self-Attention", "每个词动态关注其他词：Q×K×V", "NumPy Attention"],
    ["Transformer", "Attention + FFN 重复 N 层", "架构理解完整"],
    ["GPT", "用上文预测下一个token", "莎士比亚 GPT！"],
  ]}
/>

---

## 4. 三视角复盘

<ReviewPanel type="principle">

这 4 周我从一个神经元 → 多层网络 → 反向传播 → Transformer → 完整 GPT。我亲手写过 forward pass、backward pass、Self-Attention、GPT 训练循环。AI 对我来说不再是黑盒——我知道里面的齿轮是怎么转的。

</ReviewPanel>

<ReviewPanel type="money">

训练大模型成本百万美元起步——这不是个人开发者的战场。但理解训练过程让我知道：什么时候微调、什么时候 RAG、每个决策的成本和效果。这就是「技术判断力」——也是你区别于「只会调 API」的开发者的核心竞争力。

</ReviewPanel>

<ReviewPanel type="education">

AI 的学习过程和人类学说话惊人地相似：多听（多训练数据）→ 模仿（预测下一个字）→ 纠错（梯度下降）→ 越来越准确。给孩子讲 AI 时不需要讲数学——只需要讲「它和你小时候学说话的方式是一样的」。

</ReviewPanel>

---

## 准备进入模块五

模块五是最后一个模块——把你前四个模块积累的能力变成真正的产品。从需求验证到 MVP 到部署上线到收集反馈，跑通完整的变现闭环。你已经不是「AI 用户」了——你是「AI 产品开发者」。
